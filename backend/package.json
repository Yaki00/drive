{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node backend for React frontend (no Nest)",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": "<=16.20.2"
  },
  "scripts": {
    "build": "node -e \"console.log('No build required for node backend')\"",
    "start": "node server/server.js",
    "start:dev": "node server/server.js",
    "start:debug": "node --inspect server/server.js",
    "start:prod": "node server/server.js",
    "lint": "echo \"lint skipped for node backend\"",
    "test": "echo \"Use curl API tests\""
  },
  "dependencies": {},
  "devDependencies": {}
}
